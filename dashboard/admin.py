"""
Admin configuration for the dashboard app.
The dashboard app has no database models of its own — it aggregates data
from other apps via service functions. Nothing to register here.
"""
from django.contrib import admin  # noqa: F401
